@model IEnumerable<selahattin.Models.aboutMe>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
    <script src="https://code.jquery.com/jquery-1.11.3.min.js" type="text/javascript"></script>
<table class="table">
    <tr>
        <th>
           1.YAZI
        </th>
        <th>
            1.RESİM
        </th>
       
      
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.Raw(item.text1)
        </td>
        <td>
            <img class="img-responsive" src="~/@item.image1" width="200" height="100" />
        </td>
       
     
    </tr>
}

    <tr>
        <th>
            2.YAZI
        </th>
        <th>
            2.RESİM
        </th>

    </tr>
    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.Raw( item.text2)
            </td>
            <td>
                <img class="img-responsive" src="~/@item.image2" width="200" height="100" />
            </td>

        </tr>
        <tr>
            <td>
                @Html.ActionLink("Güncelle", "Edit", new {  id=item.id  }) |

            </td>
        </tr>
    }
</table>
